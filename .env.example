# SQLite Database (automatically created)
# No additional configuration needed for local development

# For production deployment, you can switch to PostgreSQL:
# DATABASE_URL="postgresql://username:password@localhost:5432/smart_reconciliation"